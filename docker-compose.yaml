services:
  toodledo-app:
    build: .
    ports:
      - "43000:3000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data  # 로컬의 data 폴더와 컨테이너 내부 연결 (DB 유지)
      - ./public:/app/public  # <-- 이 줄을 추가해 주세요! (HTML 실시간 동기화)
